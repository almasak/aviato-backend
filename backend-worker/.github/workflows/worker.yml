name: Worker

on:
  repository_dispatch:
    types: [process-task]

jobs:
  process:
    runs-on: ubuntu-latest
    steps:
      - name: Log task
        run: |
          echo "Processing task ${{ github.event.client_payload.taskId }}"
